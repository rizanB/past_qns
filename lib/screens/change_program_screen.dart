import 'package:flutter/material.dart';

class ChangeProgramScreen extends StatelessWidget {
  const ChangeProgramScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}